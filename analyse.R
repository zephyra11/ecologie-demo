#charger les packages
library(tidyverse)
library(ratdat)
#graphique
ggplot(data = complete_old, aes(x=weight, y=hindfoot_length)) + geom_point(color="red")

#commande pour git
#git add/ git commit -m / git status / git log
#git add analyse.R / git commit -m "ajout de la couleur/ 

